# GLM / DeepSeek 工具适配检查点

日期：2026-09-22。工作目录：`E:\git\grok2codex`，`main`。

## 当前节点

本地工具适配与隔离候选回归完成。本次按用户授权交付到
`https://github.com/pluscodex888/grok2codex` 的 `main`，仅交付源码，不部署。
本文件记录提交前验收；提交与推送状态以本地提交和 GitHub `main` 引用一致为准。
未调用真实 DeepSeek API。
既有 New-API 智能调度 Goal 仍未完成，不以本库测试代替服务端整体验收。

## 已完成节点

- 新增 `glm2codex`，与根包共同交付，不是独立发布包。
- GLM 国内、海外、自建网关端点解析；国内 Responses 使用 `/api/v1/responses`。
- DeepSeek 独立入口 `@grok2codex/client-bridge/glm/deepseek`，模型为
  `deepseek-flash`、`deepseek-v4-pro`，默认 high 推理。
- 原生 Responses 优先；仅结构化、明确的协议/端点不支持错误允许一次同源 Chat 回退。
  认证、策略、限流、配额、模型错误、超时、含糊 404、已经转发过帧的流均不回退。
- function/custom/namespace 的目录、历史、指定选择和响应统一映射；保留调用 ID、
  原始 custom input、推理和工具结果。不执行工具，不绕过原客户端审批或校验。
- DeepSeek 完整无状态历史、独立推理等级、用户标识转换、Chat 正常 `role:null`
  结束帧、无 `[DONE]` 的原生 Responses 终态已覆盖。
- 未实现的状态、内置工具及语义控制明确拒绝；不静默丢弃后宣称支持。
- 修复意外原生 custom 调用绕过封装校验、上游 namespace 覆盖原始身份的问题。
- 根包导出、类型声明、文档及打包清单接入；交付测试检查归档导出及文件哈希。

## 已验证证据

- 工作树 `npm test`：190 / 190 通过，0 失败、0 跳过；其中含原有 xAI 未提交测试。
- 精确暂存候选的隔离 `npm test`：186 / 186 通过，0 失败、0 跳过。
  候选树 `3946324e3709df6c024c414bedc3d7f0a8e61d8a` 经 `git archive` 导出后测试，
  不含原有 xAI 改动；随后仅更新本检查点，不改变已测试源码。测试使用合成样本和 mock upstream。
- TypeScript：现有本机 TypeScript 工具，`--noEmit --module nodenext
  --moduleResolution nodenext --target ES2022` 检查根导出及 GLM/DeepSeek 声明，通过。
- `git diff --check`：通过；仅 Git 行尾规范提示。
- 中间失败已聚焦修复：正常结束帧 nullable role；新增回调与公共声明的成功状态
  契约统一后更新测试预期。没有通过删除门禁掩盖失败。
- 原有 `CHANGELOG.md`、`src/http-errors.mjs`、`test/xai-auth-context.test.mjs`
  变更保留，未纳入本次自动提交。
- 交付范围：`glm2codex/` 的 18 个文件，以及根部 `README.md`、`package.json`、
  `scripts/release.mjs`、`src/index.mjs`、`src/index.d.ts`、`test/delivery.test.mjs`，共 24 个文件。
- 目标 GitHub 仓库为公开仓库；已审查交付文件，无真实凭据、业务数据、缓存或构建产物。
  现有 GitHub 登录具备管理权限，无需使用本次聊天提供的令牌。

## 唯一阻断 / 下一节点

本地实现无阻断。真实 API 与安装中的 PC 前端未验收，不属于已通过证据。
下一节点是在选定宿主中加载本库，用实际端点验证两轮工具结果续接、子代理任务、
取消和 SSE 错误；不得把现有桌面直连 Chat 测试当作 New-API 计费路径验收。

服务端待办：Go New-API / CLIProxyAPI 尚不会自动加载此 JavaScript 库；需分别核验
直接 Zhipu 渠道的协议协商、custom/namespace 历史、SSE 终态与退款/重新计费。
不得因完成本库就宣称 GPT→GLM 线上全链路等价或已经上线。

边界：原始 schema/strict 和 custom grammar 的透传不等于上游保证约束解码；客户端
原运行时校验仍须保留。DeepSeek Chat 不承诺原生 Responses 的动态注入调用能力。

官方文档与使用示例见 [README](README.md)。本文件不包含凭据、业务正文或会话日志。
